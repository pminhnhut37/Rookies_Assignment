@model Assignment.Shared.Rating.RatingVM

<h3>
    @for (int i = 0; i < Model.Value; i++)
    {
        <i class="fas fa-star" style="color: yellow"></i>
    }
</h3>
<label>@Convert.ToDateTime(Model.DateRating).ToString("dd-MM-yyyy")</label>
<p>@Model.Comment</p>